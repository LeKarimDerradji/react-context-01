import React from 'react'

// Creating a context object called ThemeContext, set the inital value to Null.
const ThemeContext = React.createContext("color")

console.log(ThemeContext);

export default ThemeContext;
